import styled from "styled-components";

export const PostsListsContainer = styled.section`
  width: 100%;
  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(auto-fit, minmax(26rem, 1fr));
  margin-bottom: 14rem;
`;
